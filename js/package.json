{
  "name": "xbinser",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm run build",
    "build": "esbuild src/lib.ts --bundle --outfile=target/lib.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@petamoriken/float16": "^3.8.7",
    "esbuild": "^0.23.1",
    "ts-node": "^10.9.2"
  }
}
